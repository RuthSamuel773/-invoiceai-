{
  "version": 2,
  "outputDirectory": "dist",
  "buildCommand": "npm install && npm run build",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
